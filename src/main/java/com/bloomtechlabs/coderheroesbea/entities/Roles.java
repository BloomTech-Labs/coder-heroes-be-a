package com.bloomtechlabs.coderheroesbea.entities;

import javax.persistence.*;

@Entity
@Table(name = "roles")
public class Roles {
    private long role_id; //table.increments('role_id');
    private String role_name; //table.string('role_name').notNullable().unique();

    /**
     * Constructor.
     * Default constructor is required to have Hibernate initialize the entity.
     */
    public Roles() {

    }

    /**
     * Constructor with role_id.
     * @param role_id - primary key (auto-increments, generated by database)
     */
    public Roles(long role_id) {
        this.role_id = role_id;
    }

    /**
     * Constructor with all fields.
     * @param role_id - primary key (auto-increments, generated by database)
     * @param role_name - string (not nullable, unique)
     */
    public Roles(long role_id, String role_name) {
        this.role_id = role_id;
        this.role_name = role_name;
    }

    @Id
    @Column(name = "role_id")
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    public long getRole_id() {
        return role_id;
    }

    public void setRole_id(long role_id) {
        this.role_id = role_id;
    }
    @Column(name = "role_name", nullable = false, unique = true)
    public String getRole_name() {
        return role_name;
    }

    public void setRole_name(String role_name) {
        this.role_name = role_name;
    }
}
